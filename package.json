{
  "name": "flashcard-guru-pages",
  "version": "0.1.0",
  "description": "Flashcard Guru marketing site + legal pages, deployed to flashcard-guru.flashify.app via Cloudflare Pages.",
  "private": true,
  "scripts": {
    "deploy": "wrangler pages deploy public --project-name=flashcard-guru",
    "deploy:preview": "wrangler pages deploy public --project-name=flashcard-guru --branch=preview",
    "dev": "wrangler pages dev public --port 8788"
  },
  "devDependencies": {
    "wrangler": "^4.90.0"
  }
}
